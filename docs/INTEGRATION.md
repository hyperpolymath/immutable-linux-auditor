# Integration Targets

This auditor can be delivered as:

1. KDE Discover plugin (QML + libdiscover)
2. GNOME Software plugin (GTK/Adwaita + gnome-software plugin API)
3. Standalone app (QML or GTK)

Selected: Standalone app (QML/Qt).
