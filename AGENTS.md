# Repo Guidelines

- This repo is new; keep changes minimal and document assumptions.
- Prefer ASCII-only content unless a file already uses Unicode.
- Add or update docs when making behavioral changes.
- Use `rg` for search and keep edits scoped to this repo.
